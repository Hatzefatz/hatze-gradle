project.buildscript {
    repositories {
        maven {
            url = uri("https://plugins.gradle.org/m2/")
        }
    }
    dependencies {
        classpath("org.hidetake.ssh:org.hidetake.ssh.gradle.plugin:2.11.2")
    }
}